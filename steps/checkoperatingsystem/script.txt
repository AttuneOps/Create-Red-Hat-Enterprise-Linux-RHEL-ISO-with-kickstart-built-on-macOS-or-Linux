if ! hostnamectl | \
    grep "Operating System" | \
    grep --quiet "{kickstartedOperatingSystemName}"
then
    echo "This is not {kickstartedOperatingSystemName}."
    exit 1
else
    echo "This is {kickstartedOperatingSystemName}."
fi