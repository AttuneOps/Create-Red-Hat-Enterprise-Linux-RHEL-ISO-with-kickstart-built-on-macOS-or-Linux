cd /sys/firmware

if ls | grep --quiet "efi"
then
    echo "This looks like UEFI boot."
    if [ {kickstartedBootLoaderIsUefi} != '1' ]
    then
        exit 1
    fi
else
    echo "This looks like BIOS boot."
    if [ {kickstartedBootLoaderIsBios} != '1' ]
    then
        exit 1
    fi
fi